build/
.vscode/
.idea/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/
*.egg-info/

# Dev artifacts that shouldn't ship
docs/_probe.py
*.log
*_log.txt
# Stale standalone-test screenshots from the v0 scaffold; canonical
# scene captures live under docs/img/dash-*.png
docs/idle.png
docs/prompt.png
docs/sessions.png

# OS
.DS_Store
Thumbs.db

tools/web/wasm/build/
tools/web/static/dash_datalayer.mjs
tools/web/static/dash_datalayer.wasm
.superpowers/
.playwright-mcp/
step2-browser-smoke.png
fonts/zh.ttf
fonts/_gb2312.txt
main/zh.ttf
# Consolas UI subsets (derived from Windows system fonts — regenerate with
# tools/make_ui_font.py)
fonts/ui.ttf
fonts/ui_bold.ttf
main/ui.ttf
main/ui_bold.ttf
# clock_digits.ttf source (3.6MB OFL original; the committed 2.4KB subset
# is regenerated from it via tools/make_clock_font.py)
fonts/MPLUSRounded1c-Black.ttf

# Device screenshots — throwaway esp-harness screenshot/verify captures. They
# pile up in the repo root (default --out) and under .harness/; neither is
# source. Canonical, curated scene images are committed under docs/img/ (NOT
# ignored). See AGENTS.md "Screenshots". Route new captures to a scratch path
# or into .harness/ — never commit them.
.harness/
/*.png
