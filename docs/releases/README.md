# Release notes

Per-version notes for every tagged release. The canonical project-wide
log lives in [`../../CHANGELOG.md`](../../CHANGELOG.md); these pages expand each
version into a readable engineer-facing summary with code, file
references, and the framework gaps each release closed.

## Versions

- [v0.1.1](./v0.1.1.md) — v1 firmware + bridge integration (multi-agent slots, TCP transport, `~/.claude-buddy/config.toml`, stress suite 5/5 at 99 snapshots/s).
- [v0.1.0](./v0.1.0.md) — initial public release (5 LVGL scenes, host bridge with `SessionRegistry`, CI replay, GitHub Pages homepage).

For what's coming next, see [`../../ROADMAP.md`](../../ROADMAP.md) (`v0.2.0` →
`v2.2.0`).
